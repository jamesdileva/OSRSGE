/**
 * Scheduler stub — the auto-refresh scheduler (roadmap Sprint 10,
 * implementation guide §41) will live here. Kept as explicit no-ops
 * so main.ts already reflects the target lifecycle shape.
 */

export function startScheduler(): void {
  // No-op for Sprint 1.
}

export function stopScheduler(): void {
  // No-op for Sprint 1.
}
